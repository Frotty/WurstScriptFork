before_install:
  - source "$HOME/.sdkman/bin/sdkman-init.sh"
  - sdk install java 25-tem
  - sdk use java 25-tem
  - rm -rf "$HOME/.sdkman/archives/"* "$HOME/.sdkman/tmp/"* || true
  - echo "JAVA_HOME=$JAVA_HOME"
  - java -version
